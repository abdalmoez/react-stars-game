import React from 'react';

const Star = (props) =>{
    if(props.visible==false||false)
        return <></>;
    else
        return (
        
    <i className="star icon big"></i>

        );
    
  }
  
  export default Star;